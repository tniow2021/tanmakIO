바이너리->"해석자->공유구조체->구조체별 버퍼(큐), 변환기로 아웃리턴하는 수많은 Receive함수들"->인게임넷에서 호출해서 읽어들임->게임용 구조체 

해석자: 바이너리에 있는 구조체 구분코드를 통해 걸맞는 구조체로 바꿈.


인디코딩용 구조체, 해석기, 구조체 코드는 한몸이어야할듯.

UserTransForm.ToV3같은 함수는 만들지 말고 별개로 다른 변환기라는 정적 클래스에다 만들 것. 
왜냐하면 해석자 클래스 자체는 서버와 클라가 공유해야하기 때문.
그래서 클라에서 TransForm 데이터를 보낼때에는 GetNetwork().Send(해석기.UserTransForm(변환기.ToUserTransForm(transform))


@@ 데이터+데이터구분코드@@











큐에 대한 것.